use clap::ArgMatches;

pub fn salvage(matches: &ArgMatches) {
    //GU-AG rule (a.k.a. "the Canon Splice Site Salvation Rule")
    //Must be on a same strand of reference genome
    //Must have one included and one not included
    
}
